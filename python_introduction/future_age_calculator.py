# Set number of hours
hours = 2

# Convert hours to seconds
seconds = hours * 3600

# Print the result using simple string + number conversion
print(str(hours) + " hour(s) is " + str(seconds) + " seconds.")
